[package]
name = "addrust-ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["staticlib"]

[dependencies]
addrust = { path = "../addrust", default-features = false }

[profile.release]
opt-level = "z"
strip = true
